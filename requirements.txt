# --- PDF parsing ---
pdfplumber

# --- OCR for pages with no text layer ---
# Needs the tesseract binary too:
#   macOS: brew install tesseract
#   Linux: apt-get install tesseract-ocr
# Without it the app still runs; scanned pages are reported as unreadable.
pytesseract

# --- Chunking (LangChain) ---
langchain
langchain-openai
langchain-text-splitters

# --- Vector DB ---
chromadb

# --- API ---
fastapi
uvicorn[standard]
python-multipart

# --- UI ---
streamlit

# --- Config ---
python-dotenv

# --- Dev ---
pytest
black
ruff
